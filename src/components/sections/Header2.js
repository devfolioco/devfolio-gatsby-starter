import React from 'react';

import { Container} from '@components/global';

const Header2 = props => (
  <header style={{ padding: '1rem 0', marginTop: '2rem' }}>
    <Container>
          <p>
            Idea Generation page
          </p>
    </Container>
  </header>
);

export default Header2;
